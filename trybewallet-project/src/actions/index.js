export const emailAction = (email) => ({ type: 'EMAIL_ACTION', email });

export const currenciesActionThunk = (currencies) => ({ type: 'CURRENCIES_ACTION', currencies});

export const expensesAction = (expenses) => ({ type: 'EXPENSES_ACTION', expenses });

export const attAction = (attButton) => ({ type: 'BUTTON_ATT', attButton });

export const saveAction = (saveButton) => ({ type: 'BUTTON_SAVE', saveButton });